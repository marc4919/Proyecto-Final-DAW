<form method="post" action="{{ path('app_musical_delete', {'id': musical.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ musical.id) }}">
    <button class="btn">Delete</button>
</form>
