<form method="post" action="{{ path('app_participante_delete', {'id': participante.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ participante.id) }}">
    <button class="btn">Eliminar</button>
</form>
